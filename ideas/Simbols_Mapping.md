`\` could be replaced by 
$$ \lambda $$

`^a` could be replaced by 
$$\hat a$$

`<-` could be replaced by
$$\leftarrow$$

`->` could be replaced by
$$\mapsto$$

`=>` could be replaced by
$$\Rightarrow$$

`==` could be replaced by
$$\equiv$$

`null` could be replaced by
$$\emptyset$$

`#` could be replaced by
$$ \because $$